<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <!-- Uncomment this to trust all certificates (not recommended for production) -->
            <!--<certificates src="system" />-->
        </trust-anchors>
    </base-config>
</network-security-config>
